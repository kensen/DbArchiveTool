SET XACT_ABORT ON;
SET NOCOUNT ON;
BEGIN TRY
    BEGIN TRAN;
    ALTER PARTITION FUNCTION [{PartitionFunction}]() MERGE RANGE ({BoundaryLiteral});
    COMMIT TRAN;
END TRY
BEGIN CATCH
    IF @@TRANCOUNT > 0 ROLLBACK TRAN;
    DECLARE @ErrorMessage NVARCHAR(4000) = ERROR_MESSAGE();
    RAISERROR (''分区合并失败: %s'', 16, 1, @ErrorMessage);
END CATCH;
