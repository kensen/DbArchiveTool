@page "/fetchdata"
@using AntDesign
@attribute [ReuseTabsPage(Title = "接口样例", Closable = true, Order = 10)]

<h1>归档 API 调用示例</h1>

<p>前端将在后续迭代中通过 <code>ArchiveApi</code> HttpClient 调用 <code>/api/v1/archive-tasks</code> 等接口。当前页面仅保留占位说明。</p>

<ul>
    <li>使用 Ant Design Blazor 组件构建数据网格与筛选面板。</li>
    <li>调用应用层写入服务以新建归档任务。</li>
    <li>结合 SignalR/SSE 实现任务状态实时刷新。</li>
</ul>
